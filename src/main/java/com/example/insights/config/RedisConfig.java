package com.example.insights.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class RedisConfig {
    // Spring Boot auto-configures RedisTemplate via application.yml
}
